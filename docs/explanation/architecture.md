# 架构概览

## 系统架构

```
┌─────────────────────────────────────────────────────┐
│                    Frontend (React 19)                │
│  ┌───────────┐ ┌──────────┐ ┌──────────────────┐   │
│  │ Dashboard  │ │ Industry │ │ Portfolio        │   │
│  │ 市场概览   │ │ 产业情报  │ │ 模拟组合         │   │
│  ├───────────┤ ├──────────┤ ├──────────────────┤   │
│  │ Industry  │ │ 估值模型  │ │ Companies        │   │
│  │ 产业链全景 │ │ 配置方案  │ │ 公司列表         │   │
│  └───────────┘ └──────────┘ └──────────────────┘   │
│                        │                              │
│              ┌─────────┴─────────┐                    │
│              │   api.js 客户端   │                    │
│              └─────────┬─────────┘                    │
│                        │ HTTP /api/* (Vite 代理)      │
└────────────────────────┼────────────────────────────┘
                         │
┌────────────────────────┼────────────────────────────┐
│              ┌─────────┴─────────┐                    │
│              │  FastAPI Backend   │                    │
│              │   (uvicorn :8001)  │                    │
│              └─────────┬─────────┘                    │
│                        │                                │
│  ┌─────────────────────┼─────────────────────┐        │
│  │   API 路由层 (main.py)                    │        │
│  │   ~30+ 端点，16 个功能模块               │        │
│  └─────────────────────┼─────────────────────┘        │
│                        │                                │
│  ┌─────────────────────┼─────────────────────┐        │
│  │   业务逻辑层                                 │        │
│  │  ┌──────────┐ ┌────────────┐ ┌──────────┐  │        │
│  │  │valuation │ │valuation_v2│ │price_data│  │        │
│  │  │GGM 估值   │ │供需PE估值   │ │多源行情   │  │        │
│  │  └──────────┘ └────────────┘ └──────────┘  │        │
│  │  ┌──────────┐ ┌────────────┐                │        │
│  │  │ai_analysis│ │scheduler   │                │        │
│  │  │AI 分析生成 │ │定时采集    │                │        │
│  │  └──────────┘ └────────────┘                │        │
│  └─────────────────────┼─────────────────────┘        │
│                        │                                │
│  ┌─────────────────────┼─────────────────────┐        │
│  │   数据访问层                                 │        │
│  │  ┌──────────┐ ┌────────────┐                │        │
│  │  │ SQLAlchemy│ │ models.py  │                │        │
│  │  │ ORM      │ │ 22+ 数据表 │                │        │
│  │  └──────────┘ └────────────┘                │        │
│  └─────────────────────┼─────────────────────┘        │
│                        │                                │
│              ┌─────────┴─────────┐                    │
│              │  SQLite Database   │                    │
│              │ teck_dashboard.db  │                    │
│              └───────────────────┘                    │
│                                                        │
│  ┌────────────────────────────────────────────┐       │
│  │  行业数据采集器 (industry_collector/)        │       │
│  │  ┌──────────┐ ┌──────────┐ ┌────────────┐  │       │
│  │  │ nvidia_ir │ │ tsmc_ir  │ │ trendforce  │  │       │
│  │  │ asml_ir   │ │ semi_org │ │ wsts_sia    │  │       │
│  │  │ ...共14源 │ │          │ │             │  │       │
│  │  └──────────┘ └──────────┘ └────────────┘  │       │
│  └────────────────────────────────────────────┘       │
└────────────────────────────────────────────────────────┘

外部数据源:
┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐
│ 腾讯财经  │ │ yfinance  │ │ akshare  │ │ Naver    │
│ API      │ │ (美股)    │ │ (A股/港) │ │ (韩国)   │
└──────────┘ └──────────┘ └──────────┘ └──────────┘
```

## 核心设计原则

### 1. 多数据源自动切换

价格数据采用"优先级链"策略：对每只股票依次尝试可用数据源，直到成功返回数据。

- **美股**: 腾讯财经 API（优先）→ yfinance（备选）
- **A股**: akshare（优先）→ yfinance（备选）
- **港股**: akshare（优先）→ yfinance（备选）
- **韩国股**: Naver Mobile API（优先）→ FinanceDataReader → yfinance

参见 `price_data.py` 中的 `fetch_price_history()` 函数。

### 2. 缓存第一

所有外部 API 数据先写入 SQLite 缓存，下次读取时优先返回缓存数据。即使用户离线或 API 故障，仪表盘仍可展示上次成功拉取的数据。

- `PriceCache` 表缓存历史价格
- `StockInfoCache` 表缓存公司信息

### 3. 产业链中心的数据模型

不是以"公司"而是以"产业链环节"为中心组织数据。每家公司通过 `CompanyChainLink` 关联到 1 个或多个环节，支持多角色标注（如三星既在存储芯片又在晶圆代工）。

### 4. 两代估值模型并存

| 特性 | v1 (Gordon Growth) | v2 (供需感知未来PE) |
|------|-------------------|-------------------|
| 方法论 | DCF 两阶段折现 | 未来 PE 倍数法 |
| 核心输入 | 增长率、净利率、折现率 | 增长率、供需分数 |
| 供应端感知 | 无 | 供应链供需缺口 → 调整增长假设 |
| 输出 | 公允PE、公允市值、上涨空间 | 未来PE、估值信号(低估/高估) |
| 最佳用途 | 行业中性估值 | 周期性行业、供需拐点 |

参见 `docs/explanation/valuation.md`。

### 5. 产业情报的信号系统

14 个行业数据采集器定时抓取公开数据（ASML/NVIDIA/TSMC 官方 IR、TrendForce、WSTS/SIA 等），结合 DeepSeek AI 自动生成边际变化解读，经 Tier 分层（P0/P1/P2）推送给用户。

---

## 技术栈

| 层级 | 技术 | 版本 |
|------|------|------|
| 前端框架 | React | 19 |
| 构建工具 | Vite | 8+ |
| 路由 | React Router | 7 |
| 图表 | Recharts | 3 |
| 后端框架 | FastAPI | 0.115+ |
| ORM | SQLAlchemy | 2.0+ |
| 数据库 | SQLite | - |
| 数据源 | yfinance / akshare / 腾讯财经API / Naver API | - |
| AI 分析 | DeepSeek API | - |
